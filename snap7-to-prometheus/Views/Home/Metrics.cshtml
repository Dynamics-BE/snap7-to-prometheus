@using snap7_to_prometheus.Models
@model Configuration
@foreach (var db in Model.DBReads)
{
    @foreach (var tag in db.Tags)
    {
        @Html.Raw(tag.MetricsName)@Html.Raw(" ")@Html.Raw(tag.Data)@:
    }
}