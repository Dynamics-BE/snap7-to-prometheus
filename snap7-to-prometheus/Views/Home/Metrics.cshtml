@using snap7_to_prometheus.Models
@model Configuration
@foreach (var db in Model.DBReads)
{
    @foreach (var tag in db.Tags.Where(t => t.MetricsName != null))
    {
        @Html.Raw(tag.FormattedMetricsNameWithLabels)@Html.Raw(" ")@Html.Raw(tag.Data)@:
    }
}